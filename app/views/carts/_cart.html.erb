<ul>
<% @carpros.each do |carpro| %>
<li><%= carpro.product.name %> $<%= carpro.product.price %> <%= link_to "Remove",
            carpro_path(carpro),
            method: :delete  %> </li>
<% end %>
</ul>

Total value: $<%= @sum %>
